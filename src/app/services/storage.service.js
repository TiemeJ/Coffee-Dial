export const createStorageService = ({
    storage,
    ref,
    uploadBytes,
    getDownloadURL,
    deleteObject
}) => ({
    storage,
    ref,
    uploadBytes,
    getDownloadURL,
    deleteObject
});
